# Docs for the arcpro ros2 examples! 

Housed inside here is the example code for the waypointer and RL navigation. 

# Deploying

Deploy locally:
```bash
npx quartz build --serve
```
Deploy to prod (required to be member of repo):
```bash
npx quartz sync
```
