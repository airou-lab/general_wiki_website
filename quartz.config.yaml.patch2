--- quartz.config.yaml
+++ quartz.config.yaml
@@ -282,10 +282,10 @@
     folder:
       exclude:
         - reader-mode
-      positions:
-        right: []
     tag:
       exclude:
         - reader-mode
+      #positions:
+      #  right: []
 EOF
patch quartz.config.yaml < quartz.config.yaml.patch2
